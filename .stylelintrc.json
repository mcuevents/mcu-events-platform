{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "custom-variant", "layer", "apply", "plugin"]
      }
    ],
    "import-notation": null
  }
}
